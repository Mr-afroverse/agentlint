# agentlint

This is a namespace alias package. It installs [`instruction-lint`](https://pypi.org/project/instruction-lint/), the agentlint CLI.

```
pip install agentlint
agentlint --help
```

See the full project at https://github.com/Mr-afroverse/agentlint.
